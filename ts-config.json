{
   "deploy-key": "<your deploy key>",
   "agent_type": "i",
   "ruleset": "Base Rule Set, Docker Rule Set, Kubernetes Rule Set",
   "configuration":
   {
      "enable_containers":1,
      "enable_kubes":1,
      "log_level": "info"
   }
}
